Place your floor plan images here (e.g. plan.jpg, plan.png, plan.svg).

They are served at /web/floor-plan/<filename> when the app uses base /web/.

In the 2D editor, use "Use bundled sample" or set background URL to that path after deployment.
